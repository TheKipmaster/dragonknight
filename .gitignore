node_modules/
dist/
tmp/
*.log
.DS_Store
PROMPT.md

# Regenerable dev aids from scripts/gen-tiles.py + scripts/preview-maps.py
# (the game loads stone.png and the .tmj files; these are just for eyeballing)
public/maps/*.preview.png
public/tiles/*.x8.png
public/*.tiled-session

# Local Claude config/state
.claude/

# Sandbox-injected host dotfiles (masked as /dev/null character devices)
.bash_profile
.bashrc
.gitconfig
.gitmodules
.idea
.mcp.json
.profile
.ripgreprc
.vscode
.zprofile
.zshrc